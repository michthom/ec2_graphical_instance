echo ""
echo "######################################################"
echo "Installling NGinx at: `date --iso-8601=minutes`"
echo "######################################################"

# Elapsed time: Approximately ?? minutes

# Install NGinx default listening on port 80 as a canary test

dnf -y install nginx

systemctl enable nginx
systemctl start nginx
